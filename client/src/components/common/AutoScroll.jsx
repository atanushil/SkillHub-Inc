

const AutoScroll = ({direction}) => {
  
  return (
    <div class="h-14 mt-2 bg-blue-500 text-center items-center flex w-full">
    from auto scroll{direction}
    </div>
  );
};

export default AutoScroll;
